name: Deploy to dev

on:
  push:
    branches: 
      - development

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: UKHSA-Internal/webhook-action-private@v1.0
        with:
          url: ${{ secrets.DEVPUBLISHPROFILE }}
          data: >
            {"command": "publish"}


