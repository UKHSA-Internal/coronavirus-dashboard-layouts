name: Deploy to dev

on:
  push:
    branches: 
      - development

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: distributhor/workflow-webhook@v2
        env:
          webhook_url: ${{ secrets.DEVPUBLISHPROFILE }}
          data: '{"command": "publish"}'
